package com.kafka;

/**
 * Created by aturbillon on 25/04/2017.
 */
//TODO create a function that can create our cluster of 3 (or more) kafka brokers with a given set of settings
public class ClusterCreation {
    String SERVER_CONFIG ="";

    public void ClsuterCreation() {
/*
        String kafkaValue = SERVER_CONFIG;


        Properties brokerConfig = new Properties();
        brokerConfig.setProperty("broker.id", "1");
        brokerConfig.setProperty("host.name", HOST_NAME);
        brokerConfig.setProperty("port", PORT_NBR);
        brokerConfig.setProperty("log.dirs", LOG_DIRECTORY);
        brokerConfig.setProperty("log.flush.interval.messages", "100000");

    KafkaServer server1 = new KafkaServer(new KafkaConfig(brokerConfig));
*/

    }

}
